# ADR-0001: ReAct 模式作为 Agent 执行引擎

Agent 采用 ReAct（推理-行动）模式而非 Plan-then-Execute 作为核心执行循环。原因：在终端交互场景中，LLM 需要在获取信息后立即决策下一步，ReAct 的"思考→行动→观察"自然循环更适合逐步推理的任务。Plan-then-Execute 要求 LLM 预先规划所有步骤，对于工具调用结果不确定的场景（如文件搜索、命令执行结果未知）过于脆弱。后续若要引入规划能力，应在 ReAct 基础上叠加，而非替换。
